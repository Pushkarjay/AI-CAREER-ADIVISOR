# Environment Configuration Template
# Copy this file to backend/.env and frontend/.env and fill in your actual values
# NEVER commit the actual .env files with real secrets

# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (backend/.env)
# =============================================================================

# Application Settings
APP_NAME=AI Career Advisor Backend
APP_VERSION=1.0.0
DEBUG=True
LOG_LEVEL=INFO

# API Configuration
API_V1_STR=/api/v1
SECRET_KEY=your-super-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60
ALGORITHM=HS256

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000","https://your-frontend-domain.com"]

# Google Cloud Platform Configuration
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json

# Firebase Configuration
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_ADMIN_CREDENTIALS=./firebase-admin-key.json
FIREBASE_TOKEN=your-firebase-refresh-token

# Google Gemini AI
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-pro
GEMINI_TEMPERATURE=0.7

# BigQuery Settings
BIGQUERY_DATASET=career_advisor
BIGQUERY_LOCATION=US

# Firestore Settings
FIRESTORE_DATABASE=(default)

# Document AI Configuration
DOCUMENT_AI_PROCESSOR_ID=projects/your-project/locations/us/processors/your-processor-id
DOCUMENT_AI_LOCATION=us

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES (frontend/.env)
# =============================================================================

# Backend API Configuration
VITE_API_BASE_URL=http://localhost:8000
VITE_API_VERSION=v1

# Firebase Configuration (Web App)
VITE_FIREBASE_API_KEY=your-firebase-web-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-firebase-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-firebase-app-id
VITE_FIREBASE_MEASUREMENT_ID=your-analytics-measurement-id

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================

# 1. Replace all "your-*" placeholders with actual values
# 2. Get Firebase config from Firebase Console > Project Settings > General > Your apps
# 3. Get Google Cloud credentials from Google Cloud Console > IAM & Admin > Service Accounts
# 4. For local development, set VITE_API_BASE_URL to http://localhost:8000
# 5. For production, use your deployed backend URL
# 6. Never commit files containing real secrets to version control